name: ci-04

on:
  push:
    paths:
      - "04/**"
      - ".github/workflows/ci-04.yml"
  pull_request:
    paths:
      - "04/**"
      - ".github/workflows/ci-04.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "04"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install ruff
        run: python -m pip install ruff
      - name: Lint
        run: ruff check homework
      - name: Tests
        run: python homework/test.py
