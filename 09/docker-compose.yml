services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      MODEL_PATH: /app/model/iris_rules.json
      LOG_LEVEL: INFO
      PORT: "8000"
      AUTH_SECRET_KEY: change-me
      AUTH_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: "30"
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin
      USER_USERNAME: user
      USER_PASSWORD: user
